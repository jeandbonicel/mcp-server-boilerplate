services:
  mcp-server:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - MCP_HTTP_HOST=0.0.0.0
      - MCP_HTTP_PORT=3000
    restart: unless-stopped
